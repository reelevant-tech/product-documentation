---
title: 'Array Get'
description: 'Obtenir un élément d'un tableau par son index'
---

## Présentation

La transformation `array-get` récupère un élément d'un tableau par son index.

## Syntaxe

```text
array-get(<variable>, index=0)
```

## Paramètres

| Paramètre | Type | Requis | Défaut | Description |
|-----------|------|--------|--------|-------------|
| `variable` | array | Oui | - | Le tableau dont on souhaite extraire un élément |
| `index` | number | Non | `0` | L'index de l'élément à récupérer |

## Exemples

```text
array-get(<variable>, index=0)
array-get(<variable>, 2)
```
